#!/usr/bin/env bash

brew cleanup &
mise cache clean &
rm-ds-store &

declare -a home_files=(".bash_history" ".duckdb_history" ".python_history" ".viminfo")
for file in "${home_files[@]}"; do
	file="${HOME}/${file}"
	if [[ -f $file ]]; then
		rm "$file"
	fi
done

wait
