quit-after-last-window-closed = true

# ansi colors
# theme = Catppuccin Latte
# theme = Catppuccin Frappe
# theme = Catppuccin Macchiato
theme = Catppuccin Mocha
# theme = TokyoNight Storm
# theme = nord
# theme = Atom One Dark

# font
font-family = MonaspiceNe Nerd Font Mono
# font-family = MonaspiceKr Nerd Font Mono
# font-family = MonaspiceRn Nerd Font Mono

cursor-opacity=0.5

# no confirmation on close, use tmux!
confirm-close-surface = false

# enable ligatures
font-feature = ss01
font-feature = ss02
font-feature = ss03
font-feature = ss04
font-feature = ss05
font-feature = ss06
font-feature = ss07
font-feature = ss08
font-feature = ss09
font-feature = calt
font-feature = liga

font-size = 16
# adjust-cell-height = 6
# adjust-font-baseline = 0

# background
# background-opacity = 1
# background-blur-radius = 20

window-padding-x = 0
window-padding-y = 8,0
# window-padding-color = extend

macos-titlebar-style = hidden
macos-dock-drop-behavior = window

macos-icon = custom-style
macos-icon-frame = plastic
macos-icon-ghost-color = black
macos-icon-screen-color = black

window-vsync = false
window-inherit-working-directory = false

# keybindings
# keybind = clear
keybind = cmd+ctrl+r=reload_config
keybind = cmd+t=ignore
# keybind = ctrl+a>ctrl+a=text:\x01
# # keybind = ctrl+a>c=new_window
# keybind = ctrl+a>c=new_tab
# keybind = ctrl+a>p=previous_tab
# keybind = ctrl+a>n=next_tab
# keybind = ctrl+a>.=last_tab
# # goto_tab
# # move_tab
# keybind = ctrl+a>shift+p=move_tab:-1
# keybind = ctrl+a>shift+n=move_tab:1
# # toggle_tab_overview
#
# keybind = ctrl+a>shift+\=new_split:left
# keybind = ctrl+a>-=new_split:down
# keybind = ctrl+a>shift+-=new_split:up
# keybind = ctrl+a>\=new_split:right
#
# keybind = ctrl+a>backspace=close_surface
#
# keybind = ctrl+a>h=goto_split:left
# keybind = ctrl+a>j=goto_split:bottom
# keybind = ctrl+a>k=goto_split:top
# keybind = ctrl+a>l=goto_split:right
#
# keybind = ctrl+a>m=toggle_split_zoom
# # keybind = ctrl+a>=resize_split
# keybind = ctrl+a>;=equalize_splits
# # inspector
# # open_config
# keybind = ctrl+a>shift+r=reload_config
# # keybind = toggle_quick_terminal
