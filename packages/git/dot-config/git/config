[include]
	path = credentials
	path = delta
	path = difftool

[advice]
	addEmptyPathSpec = false
	pushNonFastForward = false
	# statusHints = false
	skippedCherryPicks = false
	mergeConflict = false

[branch]
	autosetupmerge = always
	; autosetuprebase = always

[init]
	defaultBranch = main

[status]
	branch = true
	showStash = true
	showUntrackedFiles = all

[commit]
	; gpgSign = true
	template = ~/.config/git/commit-template

[core]
	compression = 9
	whitespace = trailing-space
	preloadindex = true

[diff]
	colorMoved = zebra
	colorMovedWS = allow-indentation-change
	context = 8
	renames = copies
	interHunkContext = 10

[interactive]
    singlekey = true

[push]
	autosetupremote = true
    default = current

[pull]
	rebase = merges

[rerere]
	enabled = 1

[rebase]
	autostash = true

[merge]
	conflictstyle = zdiff3
	autostash = true

[submodule]
	recurse = true

[alias]
	graph = "log --graph --all --format=format:'%C(blue)%h%C(reset) - \
%C(green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(auto)%d%C(reset)'"
    recent = "!git for-each-ref --sort=-committerdate refs/heads --format='%(HEAD)%(color:yellow)%(refname:short)|%(color:bold green)%(committerdate:relative)|%(color:blue)%(subject)|%(color:magenta)%(authorname)%(color:reset)'|column -ts'|'"
    ignore = "!gi() { curl -sL https://www.toptal.com/developers/gitignore/api/$@ ;}; gi"
    st = "status"
	wt = "worktree"
	sm = "submodule"
	sw = "switch"
	co = "checkout"

[url "https://github.com/"]
	insteadOf = "gh:"

[url "https://gitlab.com/"]
	insteadOf = "gl:"
